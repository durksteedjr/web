{
  "name": "@tools/better-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "bash scripts/clean.sh",
    "generate": "bunx @better-auth/cli generate --output ../drizzle/src/db/schema/betterAuth.ts",
    "seed": "bun seed/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-auth": "^1.3.2"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.2",
    "@tools/drizzle": "workspace:*",
    "typescript": "^5.8.3"
  }
}
